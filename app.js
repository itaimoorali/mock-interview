/*

Statement:
Wrtie a function that takes a nested object and return the flattened version of it.

Example:
const nestedObject1 = {
	a: {
		foo: 'foo',
		bar: {
			bat: 1,
			ball: 'ball',
		}
	},
	b: 'test',
	c: {
		name: 'test'
	}
}

flattenObject(nestedObject1)
should return {
	foo: 'foo',
	bat: 1,
	ball: 'ball',
	b: 'test',
	name: 'test'
}

*/

export function flattenObject() {
	// code here... Hello There
}
